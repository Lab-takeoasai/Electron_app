# Electron_app

GeekToolっぽく

name.json
{
  path: html path
  cmd: shell command
  interval: x sec
  position: {
    :x
    :y
    :width
    :height
  }
}

ps -amcwwwxo "command %mem %cpu" | grep -v grep | head -13
